readNRCSNCSS <- function(dataDir,
                         format = c('orginal', 'long')[1],
                         verbose = TRUE){
  #
  dataDir <- "~/Dropbox (UFL)/Research/Datasets/NRCS_NCSS_20230922/"
  message(paste('Expected zip file not found. Go here and download the sqlite zip file. https://ncsslabdatamart.sc.egov.usda.gov/database_download.aspx'))
  
}